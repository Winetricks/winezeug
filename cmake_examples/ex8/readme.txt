This explores what happens with eclipse's subversion client
subclipse when you have multiple directories in your
project, all checked out from different places, and
the root directory that contains them is not in svn.

Subclipse gets very confused by projects like this,
so if you do this, you're stuck using the commandline client.
